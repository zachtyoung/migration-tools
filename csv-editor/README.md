# CSV Parser import tool
The CSV Parser import tool is expected to take a CSV of products and modify a specific column (category string), and append a category name to all rows with a value in that column 
